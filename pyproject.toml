[project]
name = "learn-ai-looping"
version = "0.1.0"
description = "A safe practice project for learning the foundational AI work loop"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.ruff]
line-length = 100
